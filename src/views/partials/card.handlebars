{{#each arrayProductos}}
  <div class="card">
    <h2>Titulo: {{this.nombre}} </h2>
    <p>Descripción: {{this.descripcion}} </p>
    <p>Precio: {{this.precio}} </p>
    <button>Comprar</button>
  </div>
{{/each}}